@model IEnumerable<Follower_Analyzer_for_Instagram.Models.User>

@{
    ViewBag.Title = "TestListUsersObserv";
}

<h2>TestListUsersObserv</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Username)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastUpdateDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastUpdateDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.InstagramPK }) |
            @Html.ActionLink("Details", "Details", new { id=item.InstagramPK }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.InstagramPK })
        </td>
    </tr>
}

</table>

@using (Ajax.BeginForm("GetSubscribersCurrentUser", "Home",new AjaxOptions{OnComplete= "OnComplete" }))
{
  <input type="text" />
  <input type="submit" value="AddUser" />
}
<div id="ff"></div>

<script>
    function OnComplete(response) {
        console.log(response);
      
    }
</script>
